language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t rishipahari/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rishipahari/docker-react npm run test
